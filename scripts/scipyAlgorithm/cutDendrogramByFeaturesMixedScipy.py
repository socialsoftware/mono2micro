import numpy as np
from scipy.cluster import hierarchy
from sklearn import metrics
import json


def cutDendrogramByFeaturesMixed(codebasesPath, codebaseName, dendrogramName, graphName, cutType, cutValue):
    pass